---
name: commit
description: Commit completed work safely, including finalizing an approved Kanban card when applicable. Use when the user explicitly invokes `$commit`, asks to use the commit skill by name, or approves work under the Kanban workflow.
---

- Use git to commit the current changes relevent to the conversation at hand. 
- Write a breif commit title/message which explains why, not what.
- Do not commit irrelevent changes. Let the user know if there is still irrelevent diff.
